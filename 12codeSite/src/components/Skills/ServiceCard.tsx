interface ServiceCardProps {
  icon: string;
  title: string;
  desc: string;
}

export const ServiceCard = ({icon, title, desc}: ServiceCardProps) => {
    return (
        <>
            <h1>skill card</h1>
        </>
    )
};